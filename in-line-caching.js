// inline cach is an optimization for speeding v8 engine and accesses in bytecode generated by Ignition

// we have a one func and it return name 
function finduser(user){
     return`Hello ${user.name}`;
}

// we have object and Info user
const userInfo = {
    name:"afshin"
}
// we have a lot of call func and v8 just run one time then cach in other
finduser(userInfo);
// read from cach
finduser(userInfo);
finduser(userInfo);
finduser(userInfo);
finduser(userInfo);
finduser(userInfo);
finduser(userInfo);
finduser(userInfo);
finduser(userInfo);
